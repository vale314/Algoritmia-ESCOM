#include <list>
#include <iostream>

using namespace std;

int main(){
    list<int> numerosInt;

    stringstream stream( s );

    int number;
    
    while ( stream >> number )
        numerosInt.push_back( number );
}